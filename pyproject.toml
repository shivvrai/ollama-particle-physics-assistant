[project]
name = "ollama-app"
version = "0.1.0"
description = "A RAG app using Ollama with cat facts dataset"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ollama>=0.6.2",
    "streamlit>=1.59.2",
    "numpy>=2.5.1",
    "pandas>=2.0.0",
    "matplotlib>=3.7.0",
]
